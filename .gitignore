# Terraform state files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Terraform plan files
*.tfplan

# Sensitive variable files (custom, if used)
*.tfvars
*.tfvars.json

# Ignore .terraform directory (provider/plugins cache, local state)
.terraform/
.terraform.lock.hcl

# Override files (e.g., environment-specific)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files (if placed in repo)
.terraformrc
terraform.rc
